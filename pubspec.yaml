name: pi_3_0101_04
description: "Omnizona - Jogo de RPG | PUC-Campinas PI III"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Geolocalização (já estava no projeto)
  geolocator: ^14.0.2

  # Firebase — adicionar FlutterFire CLI para configurar
  firebase_core: ^4.7.0
  firebase_auth: ^6.4.0
  cloud_firestore: ^6.3.0
  shared_preferences: ^2.5.5

  # Som ambiente (descomente quando tiver os assets de áudio)
  audioplayers: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/sounds/

  # fonts:
  #   - family: MedievalSharp   # exemplo de fonte temática
  #     fonts:
  #       - asset: assets/fonts/MedievalSharp.ttf
